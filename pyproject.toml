[project]
name = "mcp-servers"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["templatemcp"]

[tool.uv.workspace]
members = ["servers/templatemcp", "servers/planningmcp"]

[tool.uv.sources]
templatemcp = { workspace = true }
planningmcp = { workspace = true }
